version: "3"

services:
  knowledge:
    build: "."
    image: "knowledge:v1.0.0"
    restart: always
    container_name: "knowledge"
    ports:
      - "8086:8086"
    volumes:
      - "${PWD}/huggingface:/app/huggingface"
      - "${PWD}/conf:/app/conf"
      - "${PWD}/chroma_db:/app/chroma_db"
      - "${PWD}/knowledge.db:/app/knowledge.db"
      - "/tmp:/tmp"
